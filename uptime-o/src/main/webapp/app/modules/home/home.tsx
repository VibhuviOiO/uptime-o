import './home.scss';

import React from 'react';
import StatusPage from './components/StatusPage';

export const Home = () => {
  return (
    <div className="dashboard-home">
      <StatusPage />
    </div>
  );
};

export default Home;
