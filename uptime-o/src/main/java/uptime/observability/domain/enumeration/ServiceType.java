package uptime.observability.domain.enumeration;

public enum ServiceType {
    TCP,
    CASSANDRA,
    MONGODB,
    REDIS,
    KAFKA,
    POSTGRESQL,
    MYSQL,
    ELASTICSEARCH,
    RABBITMQ,
    CUSTOM,
}
